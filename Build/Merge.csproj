<Project>
    <PropertyGroup>
        <Configuration Condition="'$(Configuration)'==''">Debug</Configuration>
        <Version Condition="'$(Version)'==''">1.0.0</Version>
    </PropertyGroup>

    <UsingTask TaskName="Immutype.MSBuild.Tasks.Merge" AssemblyFile="Immutype.MSBuild.Tasks/bin/netstandard2.0/Immutype.MSBuild.Tasks.dll"/>
    <Target Name="Build">
        <Merge Path="../Immutype/bin/$(Configuration)" Version="$(Version)"/>
    </Target>
</Project>